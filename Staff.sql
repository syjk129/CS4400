/*CREATE TABLE IF NOT EXISTS Staff (
    username VARCHAR(30) NOT NULL,
    PRIMARY KEY (username),
    FOREIGN KEY (username) REFERENCES User(username)
        ON DELETE CASCADE    ON UPDATE CASCADE
    );*/

INSERT INTO  `cs4400_Group_60`.`Staff` (

)
VALUES 
('staff1'),
('staff2');